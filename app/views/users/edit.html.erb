 <%= form_for @user do |form| %>
    <%= render partial: '/users/form', object: form %>

    <div class="form-group">
 		 <%= form.label :about_me %>
  		<%= form.text_field :about_me, type: 'text', :class => "form-control" %>
	</div>

<% end %>